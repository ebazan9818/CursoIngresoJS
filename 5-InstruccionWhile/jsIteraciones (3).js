function mostrar()
{

var clave = prompt("ingrese el número clave.");
var intentos = 0;

while (clave !=="utn750" && intentos <2)
{
    clave = prompt ("Clave incorrecta, ingrese denuevo");
    intentos++;
    
}
}//FIN DE LA FUNCIÓN
